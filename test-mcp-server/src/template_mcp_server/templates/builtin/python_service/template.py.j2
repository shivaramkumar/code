"""Microservice module for {{ service_name }}.

Generated by MCP Template Server.
"""

SERVICE_NAME = "{{ service_name }}"
VERSION = "{{ version }}"
AUTHOR = "{{ author | default('Team') }}"
PORT = {{ port | default(8000) }}
ENABLE_METRICS = {{ enable_metrics | default(True) }}


class ServiceConfig:
    name: str = SERVICE_NAME
    version: str = VERSION
    port: int = PORT
    metrics: bool = ENABLE_METRICS


def get_status() -> dict:
    return {
        "service": SERVICE_NAME,
        "version": VERSION,
        "status": "healthy",
        "port": PORT,
        "metrics": ENABLE_METRICS,
    }
